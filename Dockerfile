Linux arch64 / arm64

Re-build grpc /protobuf locally 

Or

Install it to each container or something


May need to make a .yaml file (or a few)

Look at common/include/utils.hpp for the container port numbers for each service